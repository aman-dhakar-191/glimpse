<?xml version="1.0" encoding="utf-8"?>
<!-- Scrim behind shaped_author_name_overlay (see widget_shaped_message.xml) —
     a fixed translucent black regardless of photo content, same safe
     <shape> primitive as bg_widget_button.xml/bg_reaction_chip.xml. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/widget_photo_label_bg" />
    <corners
        android:topLeftRadius="4dp"
        android:bottomRightRadius="999dp"
        android:topRightRadius="999dp"
        android:bottomLeftRadius="4dp" />
</shape>
